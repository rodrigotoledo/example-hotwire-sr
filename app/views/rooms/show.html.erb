<p style="color: green"><%= notice %></p>

<%= turbo_frame_tag "chat_room" do %>
  <%= render @room %>
<% end %>

<div id="messages">
  <%= render @room.messages %>
</div>
<hr />
<%= link_to "New message", new_room_message_path(@room), class: 'btn btn-primary' %>